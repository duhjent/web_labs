import { Component, OnInit } from "@angular/core";

@Component({
    selector: 'app-edit-event',
    templateUrl: './edit-event.page.html'
})
export class EditEventPage implements OnInit {
    id: number;
    
    ngOnInit(): void {
        // TODO: init the id param, fetch info, etc
    }
}